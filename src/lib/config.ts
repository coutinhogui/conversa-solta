// This file is no longer the single source of truth for navigation,
// as navigation items are now defined in MainNav and MobileNav components
// to support internationalization.
// It is kept for other site-wide configurations.

export const siteConfig = {
    name: 'Shuffle Talks'
}
